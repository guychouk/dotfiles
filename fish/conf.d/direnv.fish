# Intentionally does not hook direnv.
#
# This file shadows homebrew's vendor_conf.d/direnv.fish, which would otherwise install the hook regardless.
#
# To re-enable: replace the body with `direnv hook fish | source`.
