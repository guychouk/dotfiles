# Intentionally does not hook direnv.
# This file shadows homebrew's vendor_conf.d/direnv.fish and prevents it from running direnv's hook.
# To re-enable: replace the body with `direnv hook fish | source`.
