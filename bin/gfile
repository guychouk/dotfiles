#!/bin/bash

# Download specific files from github repository
# Uses the gh cli to fetch the token, so run `gh auth login` to get it.

http -b https://api.github.com/repos/$1/contents/$2 \
	"Authorization: token $(gh config get -h github.com oauth_token)" \
	"Accept: application/vnd.github.v3.raw"
