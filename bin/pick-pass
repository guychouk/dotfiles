#!/bin/sh

prefix="${PASSWORD_STORE_DIR:-$HOME/.password-store}"
entry=$(find "$prefix" -name '*.gpg' | sed "s|^$prefix/||;s|\.gpg\$||" | choose)
if [ -n "$entry" ]; then
  pass show "$entry" | head -1 | "$HOME/bin/pbpass"
fi
