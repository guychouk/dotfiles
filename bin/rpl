#!/bin/bash

# Opens REPL in current directory

REPL="$(find "$HOME/repls" -type f | fzf)"
FILENAME=$(basename "$REPL")

if [[ -n $REPL ]]; then
	ln -s "$REPL" "$FILENAME"
	nvim "$FILENAME"
	rm "$FILENAME"
fi
