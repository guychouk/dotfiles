#!/bin/zsh

pushd ~/Projects
ls \
	| parallel 'cd {}; git remote get-url origin 2> /dev/null' \
	| awk '{match($0, /:(.*)\./, a); if(a[1]) printf "%s\n", a[1]}' \
	| fzf --bind 'enter:abort+execute(open http://github.com/{})'
popd
